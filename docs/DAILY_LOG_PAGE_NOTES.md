# Daily Log UX changes

- WeatherQuickSet component added to avoid full page reload on weather selection.
- Summary form removed inline submit button; a single sticky bottom "Günlüğü Kaydet" button submits the summary form via form attribute.
- New files:
  - src/components/WeatherQuickSet.tsx
- Edited files:
  - src/app/projects/[id]/daily-logs/[logId]/page.tsx
